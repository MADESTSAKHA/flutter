import 'package:flutter/material.dart';

class AreaOfATrapezium extends StatelessWidget {
  const AreaOfATrapezium({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
